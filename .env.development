# 开发环境配置
VITE_API_BASE_URL=/api

# Set only for a separate browser-accessible Dify URL. Never use a container-local URL.
# Leave empty to use the browser hostname without the admin-panel port.
VITE_DIFY_BASE_URL=
